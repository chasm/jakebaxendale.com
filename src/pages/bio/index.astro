---
import type { PageMeta } from "@sitebender/components/src/types";
import { Article } from "@sitebender/components";
import Layout from "../../layouts/index.astro";

export const metadata: Partial<PageMeta> = {
  author: "Charles F. Munat, coder@craft-code.dev",
  carbon: {},
  keywords: [],
  label: "Bio",
  next: "/contact",
  pageTitle: "Bio ~ Jake Baxendale",
  prev: "/",
  title: "Bio",
};

const { title } = metadata;
---

<Layout title="Bio: Jake Baxendale">
  <Article id="main-content" {title} />
</Layout>
